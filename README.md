# BlogSite

## Setup
1. Put `blog/` in `htdocs/` (XAMPP) or `www/` (WAMP)
2. Import `db.sql` in phpMyAdmin
3. Visit `http://localhost/blog/`

Admin account (admin/admin123) is created automatically on first load.

## Login
- **Username:** admin  **Password:** admin123
